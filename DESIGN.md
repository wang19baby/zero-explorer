# Zero Explorer - 设计系统

## 设计方向

**美学方向**：工业/功能优先 — 专注于功能性和数据密度，采用等宽字体元素和柔和色调。

**装饰级别**：最小化 — 让排版和间距完成所有工作，避免不必要的装饰。

**布局方法**：网格纪律 — 严格的列对齐，可预测的布局，适合多面板文件管理器。

**颜色方法**：克制 — 1个强调色 + 中性色，颜色稀少且有意义。采用 Windows 10/11 系统蓝色作为主色调。

---

## 颜色系统

### 主色调 - Windows 系统蓝
| 名称 | 值 | 用途 |
|------|-----|------|
| Primary | `#0078D4` | 主要按钮、链接、选中状态 |
| Primary Hover | `#106EBE` | 悬停状态 |
| Primary Active | `#005A9E` | 激活/按下状态 |
| Primary Light | `#E8F4FD` | 选中行背景、信息提示 |

### 中性色
| 名称 | 值 | 用途 |
|------|-----|------|
| Background | `#FFFFFF` | 主背景 |
| Secondary | `#F9F9F9` | 次要背景 |
| Tertiary | `#F3F3F3` | 工具栏、状态栏背景 |
| Border | `#E5E5E5` | 边框、分隔线 |
| Border Strong | `#D1D1D1` | 输入框边框 |
| Text Primary | `#1A1A1A` | 主文本 |
| Text Secondary | `#616161` | 次要文本 |
| Text Tertiary | `#9E9E9E` | 占位符、禁用状态 |

### 状态色
| 名称 | 值 | 用途 |
|------|-----|------|
| Success | `#0F7B0F` | 成功状态 |
| Warning | `#9D5D00` | 警告状态 |
| Error | `#C42B1C` | 错误状态 |

---

## 字体系统

### 字体家族
- **显示/标题**：Segoe UI Variable Display（Windows 原生）
- **正文**：Segoe UI Variable Text（Windows 原生）
- **数据/表格/路径**：Cascadia Code（等宽字体）

### 字体比例
| 名称 | 字体 | 大小 | 字重 |
|------|------|------|------|
| Display | Segoe UI Variable Display | 28px | 600 |
| Title | Segoe UI Variable Display | 20px | 600 |
| Subtitle | Segoe UI Variable Display | 16px | 600 |
| Body | Segoe UI Variable Text | 14px | 400 |
| Caption | Segoe UI Variable Text | 12px | 400 |
| Monospace | Cascadia Code | 13px | 400 |

---

## 间距系统

基于 4px 网格的间距系统：

| 名称 | 值 |
|------|-----|
| space-1 | 4px |
| space-2 | 8px |
| space-3 | 12px |
| space-4 | 16px |
| space-5 | 20px |
| space-6 | 24px |
| space-8 | 32px |

**密度**：紧凑 — 适合数据密集型应用，提供更高的信息密度。

---

## 圆角系统

| 名称 | 值 | 用途 |
|------|-----|------|
| radius-sm | 4px | 按钮、标签 |
| radius-md | 6px | 输入框、列表 |
| radius-lg | 8px | 卡片、面板 |

---

## 阴影系统

| 名称 | 值 | 用途 |
|------|-----|------|
| shadow-sm | 0 1px 2px rgba(0,0,0,0.05) | 标签页、小元素 |
| shadow-md | 0 4px 8px rgba(0,0,0,0.08) | 下拉菜单、弹窗 |
| shadow-lg | 0 8px 16px rgba(0,0,0,0.12) | 模态框、大弹窗 |

---

## 组件规范

### 按钮
- **高度**：32px
- **内边距**：8px 16px
- **圆角**：4px
- **字体**：14px, 500 字重

类型：
- Primary：蓝色背景，白色文字
- Secondary：白色背景，边框
- Ghost：透明背景，无边框

### 输入框
- **高度**：32px
- **内边距**：0 12px
- **圆角**：4px
- **边框**：1px solid #D1D1D1
- **聚焦状态**：蓝色边框 + 蓝色阴影

### 标签页
- **背景**：#F3F3F3
- **选中状态**：白色背景，底部蓝色边框
- **悬停状态**：白色背景

### 面包屑导航
- **位置**：Tab 上方
- **单击**：跳转到对应目录
- **双击**：切换为绝对路径输入框
- **输入路径 + Enter**：跳转到指定路径
- **Esc**：取消输入，恢复面包屑

### 文件列表
- **行高**：36px
- **列宽**：图标 32px，名称 1fr，类型 120px，大小 80px，修改时间 140px
- **悬停状态**：Primary Light 背景
- **选中状态**：Primary 背景，白色文字
- **列信息**：图标 + 名称 + 类型 + 大小 + 修改时间

---

## 布局系统

### 设计特点
- **无标题栏**：完全沉浸式体验，窗口控制通过系统手势或快捷键
- **无工具栏**：地址栏导航通过 `Ctrl+L` 唤起
- **侧边栏默认隐藏**：通过 `Ctrl+Shift+B` 切换显示

### 多面板布局
```
┌─────────────────────────────────────────────────────┐
│  ┌──────────────┬──────────────┐                    │
│  │  面板 1      │  面板 2      │  ← 无标题栏/工具栏  │
│  │  ┌────────┐ │  ┌────────┐ │                    │
│  │  │面包屑   │ │  │面包屑   │ │  ← Tab 上方       │
│  │  ├────────┤ │  ├────────┤ │                    │
│  │  │ 标签页  │ │  │ 标签页  │ │  ← Tab 下方       │
│  │  ├────────┤ │  ├────────┤ │                    │
│  │  │ 文件列表│ │  │ 文件列表│ │                    │
│  │  └────────┘ │  └────────┘ │                    │
│  └──────────────┴──────────────┘                    │
├─────────────────────────────────────────────────────┤
│  状态栏 (项目数量/选中状态/当前路径)                    │
└─────────────────────────────────────────────────────┘
```

### 侧边栏（默认隐藏）
```
┌──────────┬──────────────────────────────────────────┐
│          │                                          │
│  侧边栏   │  主内容区（多面板）                         │
│  (导航)   │                                          │
│  ┌──────┐│                                          │
│  │快速访问││                                          │
│  │此电脑 ││  ← 磁盘空间进度条                         │
│  │标签   ││  ← 用户自定义书签                         │
│  │最近   ││  ← 最近访问文件                           │
│  └──────┘│                                          │
│          │                                          │
└──────────┴──────────────────────────────────────────┘
```

**侧边栏功能**：
- **快速访问**：桌面、下载、文档、图片等常用文件夹
- **此电脑**：显示各磁盘及使用率进度条
- **标签/书签**：用户自定义收藏的文件夹，支持分组
- **最近访问**：最近打开的文件/文件夹，显示访问时间

### 面板规格
- **面板数量**：1-4 个（可动态添加/删除）
- **最小宽度**：200px
- **默认宽度**：各面板平均分配
- **可调整**：支持拖拽调整面板宽度

### 面板 Tab 规格
- **每个面板独立管理 Tab**
- **每个 Tab 对应一个独立路径**
- **支持**：新建、关闭、左右切换
- **快捷键**：`Ctrl+T` 新建，`Ctrl+W` 关闭，`Ctrl+Tab` 切换面板

**交互方式**：
- **单击 Tab 行空白区域**：新建标签页
- **双击标签页**：关闭标签页（需确认对话框）
- **至少保留一个标签页**

### 分栏布局（状态栏切换）

| 布局 | 图标 | 说明 |
|------|------|------|
| 1 分栏 | □ | 单面板，全屏显示 |
| 2 左右分栏 | ▯▯ | 左右两个面板 |
| 上下分栏 | ▯<br>▯ | 上下两个面板 |
| 3 左中右分栏 | ▯▯▯ | 左中右三个面板 |
| 上 2 下 1 | ▯▯<br>▔ | 上方两个面板，下方一个面板 |
| 上 1 下 2 | ▔<br>▯▯ | 上方一个面板，下方两个面板 |
| 4 分栏 | ▯▯<br>▯▯ | 上下两行，每行两个面板 |

**操作方式**：
- 点击状态栏中的布局图标切换
- 默认显示 1 分栏
- 面板数量不足时，多余位置留空

### 分栏模式切换

| 模式 | 图标 | 说明 |
|------|------|------|
| 多面板模式 | ▯▯ | 每个面板独立浏览，支持 Tab |
| 级联分栏模式 | ▯▯▯ | Mac 访达风格，点击文件夹右侧展开新一栏 |

**多面板模式**：
- 每个面板独立管理 Tab
- 面板之间互不影响
- 适合同时浏览多个不同目录

**级联分栏模式**：
- 点击文件夹 → 右侧展开新一栏
- 每栏显示上一级目录的内容
- 适合深层目录浏览和文件移动

### 快捷键
| 快捷键 | 功能 |
|--------|------|
| `Space` | 打开/关闭预览面板 |
| `Ctrl+Shift+B` | 切换侧边栏显示 |
| `Ctrl+T` | 新建标签页 |
| `Ctrl+W` | 关闭当前标签页 |
| `Ctrl+Tab` | 切换面板 |
| `双击面包屑` | 切换为绝对路径输入框 |

### 文件类型图标
| 类型 | 图标 |
|------|------|
| 文件夹 | 📁 |
| .rs | 🦀 |
| .js | 🟨 |
| .ts | 🔷 |
| .tsx/.jsx | ⚛️ |
| .py | 🐍 |
| .md | 📝 |
| .txt | 📄 |
| .png/.jpg/.gif | 🖼️ |
| .mp4/.avi | 🎬 |
| .mp3/.wav | 🎵 |
| .pdf | 📕 |
| .zip/.rar | 📦 |
| .exe/.msi | ⚙️ |
| 默认 | 📄 |

### 预览面板
- **触发方式**：按 `Space` 键
- **宽度**：屏幕宽度的 1/3 或 2/3（可切换）
- **显示内容**：
  - 缩略图/文件类型图标
  - 文件名
  - 类型
  - 大小
  - 创建时间
  - 修改时间
  - 访问时间
  - 完整路径
  - 内容预览（文本/代码文件）

---

## 设计原则

### 1. 工业/功能优先
专注于功能性和数据密度，采用等宽字体元素和柔和色调。避免 Q-Dir/TC 的陈旧感，同时保持专业工具的定位。

### 2. 最小化装饰
让排版和间距完成所有工作，避免不必要的装饰。颜色稀少且有意义，仅用于状态和交互反馈。

### 3. 网格纪律
严格的列对齐，可预测的布局。适合多面板文件管理器，提供一致的视觉体验。

### 4. Windows 原生集成
使用 Windows 10/11 系统字体和颜色，遵循 Fluent Design 语言，提供原生级的使用体验。

### 5. 面板独立性
每个面板都是独立的文件浏览区域，可以同时打开不同的文件夹，通过标签页自由切换。

---

## 竞品参考

### 吸收的优点
| 竞品 | 优点 | Zero Explorer 实现 |
|------|------|-------------------|
| Tessoa | 极致性能 | Rust 核心，毫秒级响应 |
| Q-Dir | 多面板布局 | 2-4 独立面板 |
| Total Commander | 双面板效率 | 现代化双面板设计 |
| Directory Opus | 功能深度 | 渐进式复杂度 |
| XYplorer | 灵活性 | 面板独立标签 |
| Files | 现代 UI | Fluent Design 美学 |

---

## 动效规范

**原则**：最小功能化 — 只有帮助理解的过渡动画。

- **时长**：100-200ms
- **缓动**：ease-in-out
- **用途**：状态变化、展开/折叠、面板切换

---

## 无障碍设计

- **对比度**：WCAG 2.1 AA 标准（4.5:1）
- **键盘导航**：完全支持 Tab/Enter/Arrow 键
- **屏幕阅读器**：适当的 ARIA 标签
- **焦点可见**：明显的焦点指示器

---

## 文件结构

```
zero-explorer/
├── DESIGN.md                    # 本文件
├── wireframes/
│   └── design-preview.html      # 设计预览页面
├── product_vision.md            # 产品愿景
├── competitive_analysis.md      # 竞品分析
└── product_analysis.md          # 产品分析
```

---

*最后更新：2026年8月31日*
